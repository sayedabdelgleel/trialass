<h1>Listing destinations</h1>

<table >
  <tr>
    <th>Name</th>
    <th>Starting price</th>
    <th>Tagline</th>
    <!-- <th>Introduction</th>
    <th>Weather months</th>
    <th>Weather temperature</th>
    <th>Weather high</th>
    <th>Weather low</th>
    <th>Activated</th>
    <th>Permalink</th> -->
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @destinations.each do |destination| %>
  <tr>
    <td><%= destination.name %></td>
    <td><%= destination.starting_price %></td>
    <td><%= destination.tagline %></td>
    <!-- <td><%= destination.introduction %></td>
    <td><%= destination.weather_months %></td>
    <td><%= destination.weather_temperature %></td>
    <td><%= destination.weather_high %></td>
    <td><%= destination.weather_low %></td>
    <td><%= destination.activated %></td>
    <td><%= destination.permalink %></td> -->
    <td><%= link_to 'Show', destination %></td>
    <td><%= link_to 'Edit', edit_destination_path(destination) %></td>
    <td><%= link_to 'Destroy', destination, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />
<%if @offset > 0%>
<%= link_to 'Previous', :controller=>"destinations" ,:offset => (@offset - @limit) %>
<%end%>
<%if @len > @limit %>
&nbsp;&nbsp;&nbsp;
<%= @page%> / <%= (@len/(@limit+0.0)).ceil%>
&nbsp;&nbsp;&nbsp;
<%end%>

<%if @offset+@limit < @len%>
<%= link_to 'Next', :controller=>"destinations"  ,:offset => (@offset+@limit) %>
<%end%>
<br />
<br />


<%= link_to 'New Destination', new_destination_path %>
