    change user name & password in database.yml
    run the following commands in command line 

    rake db:create
    rake db:migrate
    rake db:seed

    To see the site map use this url:  http://localhost:3000/sitemap.xml
    To test the project using cucumber run the following commands

    rake db:test:prepare
    cucumber
